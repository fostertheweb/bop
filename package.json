{
  "name": "bop",
  "version": "1.0.0",
  "description": "social queue",
  "scripts": {
    "web": "yarn workspace web start",
    "server": "yarn workspace server run develop",
    "build": "yarn workspace server run build && yarn workspace web run build"
  },
  "workspaces": [
    "web",
    "server"
  ],
  "private": true
}
